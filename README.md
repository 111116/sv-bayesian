## Setup

```
conda create -n mi python=3.8
conda activate mi
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
pip install mitsuba
pip install notebook matplotlib tqdm
python -m notebook
```
